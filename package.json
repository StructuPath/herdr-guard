{
  "name": "herdr-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-agent command policy layer for Herdr — watch every pane, audit, alert, interrupt",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/"
  },
  "license": "MIT"
}
